spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/camunda_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

camunda:
  bpm:
    database:
      type: postgres
    admin-user:
      id: demo
      password: demo